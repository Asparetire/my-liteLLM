# 13 - 现状评估与建议（2026-09-18）

> 本文是 2026-09-18 对全部需求完成度的盘点结论，以及下一步行动建议。
> 各需求的详细方案仍以 01-12 号文档为准，本文只记录"做到了哪、先做什么"。

## 一、完成度盘点

### 已落地

| 需求 | 完成度 | 说明 |
|---|---|---|
| 01 仓库管理与上游同步 | 结构已定 | upstream/origin 拓扑与分支铁律已在方案中确立；但本仓库（litellm-src）只有 main 一个 commit，develop 分支尚未建立 |
| 03 后端错误信息汉化 | 约 70% | `litellm_cn/` 外挂包已建成（translator.py、middleware.py、3 个测试文件）；`litellm/proxy/proxy_server.py:1529` 已有 `[CN-FORK]` 挂载点。`litellm_cn/locale/zh-CN.json` 仅 29 条译文，距 200-300 条目标还差一个量级 |
| 12 本地开发与验证环境 | 约 60% | conda 环境 litellm-cn（Python 3.13.15）+ uv 0.12.15 已装。`uv sync` 依赖安装、Docker/PostgreSQL 安装仍是用户终端待办（沙箱内 uv 无法运行） |
| （计划外）运维成果 | 已完成 | qwen3.5 200K 上下文已上线验证；Ollama prompt cache 计数补丁（deploy/ollama-litellm/patches/sitecustomize.py）；服务器同步脚本 sync-to-server.py；docs/zh-CN/ 3 份中文文档 |

### 刚起步

| 需求 | 完成度 | 差距 |
|---|---|---|
| 02 前端 UI 全面汉化 | 约 5% | 仅建了 `ui/litellm-dashboard/messages/en.json` 与 `zh-CN.json`（7 个顶层 key）。next-intl 未进 package.json、next.config.ts 未配置、i18n/ 目录不存在，即"i18n 基建（第 1 天）"尚未完成 |
| 05 计费A：配置层 Token 换算 | 待核实 | 属服务器配置层改动（input_cost_per_token 0 改 1.0），仓库内无需改码。是否已在网关生效需登录服务器核实 |

### 未开始

04 文档汉化、06 计费B（`getSpendString` 在 ui/litellm-dashboard/src/utils/dataUtils.ts:60 仍是 `$` 硬编码）、07 原生 Token 配额、08 国内 LLM 提供商、09 安全加固、10 测试 CI、11 远程配置。

## 二、建议（按优先级）

### 1. 先补齐 git 分支结构（最紧迫）

本仓库只有 main 一个 commit，没有 develop 分支，违反 CLAUDE.md 的分支铁律（main 只跟上游，自有改动全部走 develop + feat 分支）。开始写代码前先建 develop，后续所有工作走 `feat/*` 分支。

```bash
git checkout -b develop
git push origin develop
```

### 2. 处理两个仓库卫生问题（2026-09-17 调研发现，未处理）

1. `deploy/ollama-litellm/litellm_config.yaml` 被 `.gitignore:102` 忽略（上游规则，怕带密钥）。200K 上下文、模型别名这些部署配置没进版本库，只以散文件存在。建议 `git add -f` 纳入，或改成 `litellm_config.example.yaml` + 本地实文件。
2. `deploy/deploy/ollama-litellm/` 是被 git 跟踪的重复旧快照（16 个文件，含没有补丁的旧 docker-compose.yml、qwen3.5 还是 32768 的旧配置）。照错路径部署会整份用回旧配置。清理需 `git rm -r`。

### 3. 按 M0 收敛，先做 06 再铺开 02

06 改 `getSpendString()`（dataUtils.ts:50）和 `src/components/shared/table_cells/money_cell.tsx`、`spend_budget_cell.tsx` 两个集中点，两三天能完成，M0 的"无美元残留"目标可先兑现。02 的 next-intl 基建完成后再按批次推进，避免汉化铺开太早导致 UI 测试大面积红。

### 4. 需求03 有一个测试风险，需先验证

中间件改写 `error.message` 后，上游 proxy 单测可能断言英文错误原文（如 `tests/test_litellm/proxy/hooks/test_max_budget_limiter.py`、`tests/test_litellm/proxy/auth/test_auth_checks.py`）。第一次跑通环境后执行 `make test-unit-proxy-core` 时重点关注；必要时让中间件只在生产配置开启（挂载点加环境开关），不干扰测试路径。

### 5. 消除双仓库歧义

2026-09-17 记忆中主战场是 `D:\project\litellm-cn`，当前仓库 `D:\project\litellm-src` 是自它导出的副本。两处同时改动会分叉。建议明确只保留一处作为开发目录，另一处归档。

## 三、行动清单

- [x] 建 develop 分支并推送 origin（2026-09-18 完成，develop 基于初始 commit dd33036）
- [x] `litellm_config.yaml` 纳入版本库（2026-09-18 完成：以 `server-deploy/` 整目录 git add -f 纳管，`litellm_config.cn.yaml` 已核实无明文密钥）
- [x] `git rm -r deploy/deploy/ollama-litellm/` 清理旧快照（2026-09-18 完成）
- [ ] 核实网关 input_cost_per_token 是否已改为 1.0（需求05 生效；配置已写在 server-deploy/litellm_config.cn.yaml，待同步服务器生效）
- [ ] 用户终端执行 `uv sync`（配 UV_DEFAULT_INDEX 清华镜像）
- [ ] 安装 Docker Desktop，`docker compose up db -d`（只起 db）
- [ ] 跑通后执行 `make test-unit-proxy-core`，验证需求03 中间件不破坏上游单测
- [x] 开工需求06 第一步：getSpendString + money_cell + spend_budget_cell 去美元（2026-09-18 完成，commit 0b784d6，62 个相关测试全绿；decimals prop 保留兼容待第 4 步清扫）
- [x] 需求06 第二步：budgets 页面去美元（2026-09-20 完成，commit 3190c49：budget_modal/edit_budget_modal 标签与 step、BudgetTable 过滤摘要、budget_panel 副标题与删除弹窗、budgetPrecision 改整数取整、add_margin_form 去 $ 保留概念；6 个测试文件同步迁移，budgets 目录 64 测试 + 第一步回归 94 测试全绿）
- [x] 需求06 第三步：密钥/用户/团队等实体页面 + 全局 grep 收尾（2026-09-21 完成：team/user/org 域 2d1032a、usage/图表域 37a1ed6、批次5 收尾 9d3d43c，见下方进度快照）
- [ ] 开工需求02 基建：next-intl 接入 + next.config.ts 配置

### 需求06 第三步进度快照（2026-09-21 完成）

方案见 `C:\Users\Administrator\.claude\plans\git-recursive-swing.md`（批次 0-5、三个量纲甄别决策）。按领域执行，全部完成：

- 批次 0（commit 784197b）：修复 VirtualKeysTable/TeamsTable 两个红灯测试到 token 断言（第一步改 SpendBudgetCell 时未跑消费方测试的遗留）
- keys 域（commit 3cfc85e）：key_info_view 10 处 spend/budget 展示（spendDisplay 零值特判 "0 tokens"）、InheritedBudgetHint、create_key_button/key_edit_view/RegenerateKeyModal 预算 label (USD)→(tokens) 且 step 整数化、ModelMaxBudgetEditor/BudgetWindowsEditor $ 前缀改 tokens 后缀；测试同步迁移并补 key_info_view spend 侧断言（0.0001→"< 1 token"、0→"0 tokens"），全部绿
- CSV 域（commit c5e6aaa）：teamsCsvExport/EntityUsageExport/teamUserSpend 列名 (USD)/($)→(tokens)，Flat Cost 经甄别属 spend 聚合口径一并迁移；94 用例全绿
- team/user/org 域（commit 2d1032a，2026-09-20 晚落地）：Teams/TeamInfo/user_edit_view/user_info_view/OrganizationSettings 等生产文件与测试同步迁移
- usage/图表域（commit 37a1ed6，2026-09-21 验证提交）：chart_tooltip/value_formatters/EntityUsage 系列/UsagePageView/activity_metrics/per_user_usage/cost-optimization（costOptimizationUtils 拆 tokenSpend()/usd()）；46 个测试文件 629 用例全绿、无类型错误
- 批次 5 全局 grep 收尾（commit 9d3d43c，2026-09-21）：activity_metrics Total Spend 卡片、AuditLogDrawer spend/max_budget、TeamVirtualKeysTable 列头迁 token 语义；RegenerateKeyModal 与 guardrails-monitor 三测试文件为生产已改、断言遗漏的红灯，同步迁移；共 9 文件
- dev-plans/06 验收标准已修正：spend/预算语义零 $，豁免域白名单写入（模型定价、单请求成本）

**量纲甄别决策（用户拍板，落地时严格遵守）**：单请求成本（view_logs/ResponseMetrics/usage_cost.ts）保留 $；CSV 列名直接改；costOptimizationUtils 的 usd() 拆成 tokenSpend()（spend 聚合）与 usd()（classifier cost per 1K turns、eval budget 等真实 USD）
- [x] 决策保留哪个仓库目录为唯一开发目录（2026-09-18 决策：litellm-src 为唯一开发目录，litellm-cn 归档；后续开发转向服务器端副本，经 server-deploy/sync_fork.py 同步）

### 开发环境变更（2026-09-18 晚）

- 开发工作已转向服务器副本（192.168.100.123，VS Code Remote-SSH），本地 litellm-src 为提交基线
- 服务器访问 github.com 超时（实测），不能 git pull origin；代码同步走 `python server-deploy/sync_fork.py`
- 本地分支状态：develop = 52c790e；feat/06-no-usd-display = ed9fc9b（含 06 第一步），均已推送 origin
- 服务器端跑 UI 测试需先 `npm install`（npmjs.org 服务器可达，实测通过）

### 开发环境变更（2026-09-21）

- `.claude/settings.json` 新增权限白名单 `Bash(npx vitest run *)`（settings.json 被 .gitignore 忽略，仅本机生效；git 全家桶已由 settings.local.json 的 `Bash(git *)` 放行；全量 vitest 套件因不带路径仍会弹审批，符合禁跑约定）

### 汉化 UI 部署上线（2026-09-21 晚）

- 服务器 192.168.100.123:4001 容器 litellm-cn 本就健康运行（Up 2 days），未做启动动作，仅验证；master key 在服务器 `~/litellm-src/.env` 的 LITELLM_MASTER_KEY
- 管理界面此前全英文的根因：UI 是上游预构建静态产物，与后端中间件无关。已在本机构建 dashboard（`npm run build`，静态导出）并用新增 `server-deploy/deploy-ui.sh` 部署：上传 out/ 至服务器 `~/litellm-src/ui_cn`，启用 compose 中 `./ui_cn:/app/ui_cn:ro` 与 `LITELLM_UI_PATH=/app/ui_cn` 两行挂载并重建容器；`/ui/` 路由已由新产物服务（假路由 404 验证通过）
- 登录页已显示中文：LoginPage.tsx 修复 dd33036 引入的重复 `</Tooltip>` 语法错误，next-intl 未安装故用内联兜底 `t(key, fallback)`（commit 24a471d），两个登录测试文件断言同步迁移中文，15/15 绿
- 内页仍英文属需求02 范围，明日接入 next-intl 基建；compose 里 qwen3-8b 等模型配置不动
- `.gitattributes` 补 `*.sh text eol=lf`（server_init.sh 曾因 CRLF 在服务器跑挂，deploy-ui.sh 提交时已出现 LF→CRLF 警告）

创建时间：2026-09-18
